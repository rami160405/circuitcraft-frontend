// API Configuration
// This file can be modified at build time or replaced for different environments
window.API_CONFIG = {
  // Set this to your Railway backend URL when deploying
  // For local development, leave as null to auto-detect
  BACKEND_URL: null, // e.g., 'https://your-app.railway.app' or null for auto-detect
};

